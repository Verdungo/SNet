<Window x:Class="ChatClient.ChatClientWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChatClient"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="70"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Button x:Name="ConnectButton" Grid.Column="0" Grid.Row="0" Content="Connect" Click="ConnectButton_Click"></Button>
        <ListView x:Name="ChatListView" Grid.Column="0" Grid.Row="1"></ListView>
        <TextBox x:Name="Message" Grid.Column="0" Grid.Row="2"></TextBox>
        <Button x:Name="SendButton" Grid.Column="1" Grid.Row="2" Content="Send"></Button>
    </Grid>
</Window>
